const colors = (alpha = 1) => ({
  black: `rgba(30, 30, 30, ${alpha})`,
  gray: `rgba(51, 51, 51, ${alpha})`,
  white: `rgba(255, 255, 255, ${alpha})`,
  green: `rgba(0, 215, 104, ${alpha})`,
  red: `rgba(255, 0, 0, ${alpha})`
})

export default colors
